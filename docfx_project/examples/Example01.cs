// A simple example of using MyClass

var c = new MyClass();

c.Foo(() =>
    {
        // Just showing
        if (bar)
        {
            // Increase counter
            i++;
        }
    });
